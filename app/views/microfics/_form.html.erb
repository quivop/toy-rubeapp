<%= form_with(model: microfic, local: true) do |form| %>
  <% if microfic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microfic.errors.count, "error") %> prohibited this microfic from being saved:</h2>

      <ul>
      <% microfic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :microfic_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :microfic_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
